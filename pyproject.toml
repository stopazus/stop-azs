[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "stop-azs"
version = "0.1.0"
description = "Utilities for scanning iCloud task files and applying defaults"
readme = "README.md"
authors = [{name = "AutoGenerated"}]
requires-python = ">=3.10"
dependencies = []

[project.optional-dependencies]
test = ["pytest"]

[tool.pytest.ini_options]
addopts = "-ra"
testpaths = ["tests"]
